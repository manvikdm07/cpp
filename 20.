Write a C++ program that dynamically allocates memory for an array of integers using new based on the size input by the user. The program should:
-Allow the user to enter the size of the array.
-Allow the user to enter the values for the array.
-Print the array.
Free the dynamically allocated memory using delete[].




#include <iostream>
using namespace std;

int main() {
    int size;
    
    // Prompt user for the size of the array
    cout << "Enter the size of the array: ";
    cin >> size;
    
    // Dynamically allocate memory for the array
    int* arr = new int[size];
    
    // Check if memory allocation was successful (though rare in modern systems)
    if (arr == nullptr) {
        cout << "Memory allocation failed." << endl;
        return 1; // Exit with error code
    }
    
    // Prompt user to enter values for the array
    cout << "Enter " << size << " integers:" << endl;
    for (int i = 0; i < size; ++i) {
        cin >> arr[i];
    }
    
    // Print the array
    cout << "The array elements are: ";
    for (int i = 0; i < size; ++i) {
        cout << arr[i] << " ";
    }
    cout << endl;
    
    // Free the dynamically allocated memory
    delete[] arr;
    
    // Set pointer to nullptr for safety (optional but good practice)
    arr = nullptr;
    
    return 0;
}


output :
Enter the size of the array: 2
Enter 2 integers:

1 2 
The array elements are: 1 2 

